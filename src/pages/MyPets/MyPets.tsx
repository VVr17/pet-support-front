import { Typography } from "@mui/material";

const MyPets = () => {
  return (
    <Typography variant="h1" mb={5}>
      User's pets - under construction
    </Typography>
  );
};

export default MyPets;
